import './header.style.scss';

const Header = () => (
	<header className='main-header'>
		<div className='header-bar'>
			<span>LOGO</span>
			<h1>JSON BLOG</h1>
		</div>
	</header>
);

export default Header;
